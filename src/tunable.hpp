#pragma once

#include <array>
#include <cassert>
#include <cmath>
#include <functional>
#include <iostream>
#include <string>
#include <vector>

#include "types.hpp"
#include "utils.hpp"


namespace Sift {

class Tunable {
public:
    using Callback = std::function<void()>;

    Tunable() : name_(), value_(), min_(), max_(), callback_() {}
    Tunable(const std::string &name, Int32 value, Int32 min, Int32 max, Callback callback);

    constexpr operator Int32() const noexcept { return value_; }

    const std::string &name() const noexcept { return name_; }
    constexpr Int32 &value() noexcept { return value_; }
    constexpr Int32 value() const noexcept { return value_; }
    constexpr Int32 min() const noexcept { return min_; }
    constexpr Int32 max() const noexcept { return max_; }
    constexpr Float64 step() const noexcept { return std::abs(max_ - min_) / 20.0; }
    constexpr Float64 learningRate() const noexcept { return 0.002; }
    Callback callback() const noexcept { return callback_; }

    constexpr void update(Int32 value) noexcept {
        value_ = std::clamp(value, min_, max_);
        callback_();
    }

private:
    std::string name_;
    Int32 value_;
    Int32 min_;
    Int32 max_;
    Callback callback_;
};

class TunableList {
public:
    constexpr TunableList() noexcept : tunables_() {}

    static void init() noexcept;

    constexpr std::vector<Tunable *>::iterator begin() noexcept { return tunables_.begin(); }
    constexpr std::vector<Tunable *>::const_iterator begin() const noexcept { return tunables_.begin(); }
    constexpr std::vector<Tunable *>::iterator end() noexcept { return tunables_.end(); }
    constexpr std::vector<Tunable *>::const_iterator end() const noexcept { return tunables_.end(); }

    void add(Tunable &tunable) { tunables_.push_back(&tunable); }

    void update(const std::string &name, Int32 value) {
        for (Tunable *tunable : tunables_) {
            if (tunable->name() == name) {
                tunable->update(value);
                break;
            }
        }
    }

    void openBenchConfig() const noexcept {
        for (const Tunable *tunable : tunables_) {
            std::cout << tunable->name() + ", int, " + std::to_string(tunable->value()) + ", " + std::to_string(tunable->min()) + ", " + std::to_string(tunable->max()) + ", " + std::to_string(tunable->step()) + ", " + std::to_string(tunable->learningRate()) << std::endl;
        }
    }

    void weatherFactoryConfig() const noexcept {
        std::cout << "{" << std::endl;
        bool first = true;
        for (const Tunable *tunable : tunables_) {
            if (!first) {
                std::cout << "," << std::endl;
            }

            first = false;

            std::cout << "  \"" << tunable->name() << "\": {" << std::endl;
            std::cout << "    \"value\": " << tunable->value() << "," << std::endl;
            std::cout << "    \"min_value\": " << tunable->min() << "," << std::endl;
            std::cout << "    \"max_value\": " << tunable->max() << "," << std::endl;
            std::cout << "    \"step\": " << tunable->step() << std::endl;
            std::cout << "  }";
        }

        std::cout << std::endl;
        std::cout << "}" << std::endl;
    }

private:
    std::vector<Tunable *> tunables_;
};

inline TunableList TUNABLES;

inline Tunable::Tunable(const std::string &name, Int32 value, Int32 min, Int32 max, Callback callback) : name_(name), value_(value), min_(min), max_(max), callback_(std::move(callback)) { TUNABLES.add(*this); }

#if defined(SPSA_TUNE)
#define TUNABLE_CALLBACK(name, val, min, max, callback) \
    inline Tunable name##_TUNABLE = Tunable(#name, val, min, max, callback); \
    inline Int32 &name = name##_TUNABLE.value();
#else
#define TUNABLE_CALLBACK(name, val, min, max, callback) \
    static constexpr Int32 name = val;
#endif

#define TUNABLE(name, val, min, max) \
    TUNABLE_CALLBACK(name, val, min, max, []() {})


static constexpr Int32 FDEPTH_SCALE = 128;

TUNABLE(TT_REPLACE_DEPTH_OFFSET, 532, 0, 1024);
TUNABLE(TT_REPLACE_PV_SCALE, 235, 0, 768);
TUNABLE(TT_QUALITY_DEPTH_SCALE, 147, 0, 512);
TUNABLE(TT_QUALITY_AGE_DIFF_SCALE, 241, 0, 512);

static inline std::array<Int32, 7> SEE_PIECE_VALUES;

namespace SEE {

static void init();

}

TUNABLE_CALLBACK(SEE_PAWN_VALUE, 94, 50, 200, []() { SEE::init(); });
TUNABLE_CALLBACK(SEE_KNIGHT_VALUE, 437, 300, 700, []() { SEE::init(); });
TUNABLE_CALLBACK(SEE_BISHOP_VALUE, 482, 300, 700, []() { SEE::init(); });
TUNABLE_CALLBACK(SEE_ROOK_VALUE, 673, 400, 1000, []() { SEE::init(); });
TUNABLE_CALLBACK(SEE_QUEEN_VALUE, 1253, 800, 1600, []() { SEE::init(); });

static inline void SEE::init() { SEE_PIECE_VALUES = {SEE_PAWN_VALUE, SEE_KNIGHT_VALUE, SEE_BISHOP_VALUE, SEE_ROOK_VALUE, SEE_QUEEN_VALUE, 0, 0}; }

TUNABLE(TIME_BASE_TIME_SCALE, 18, 10, 40);
TUNABLE(TIME_BASE_TIME_INCREMENT_SCALE, 84, 50, 150);

TUNABLE(TIME_SOFT_BOUND_SCALE, 64, 50, 100);
TUNABLE(TIME_HARD_BOUND_SCALE, 55, 20, 100);

TUNABLE(TIME_SCALE_MATE_SCORE, 15, 1, 100);
TUNABLE(TIME_SCALE_DECISIVE_SCORE, 46, 1, 100);

TUNABLE(TIME_SCALE_NODE_BASE, 254, 150, 300);
TUNABLE(TIME_SCALE_NODE_SCALE, 185, 100, 250);
TUNABLE(TIME_SCALE_NODE_MIN, 6, 1, 100);

TUNABLE(TIME_SCALE_STABILITY_BASE, 72, 40, 100);
TUNABLE(TIME_SCALE_STABILITY_MAX, 289, 120, 1000);
TUNABLE(TIME_SCALE_STABILITY_SCALE, 919, 200, 1500);
TUNABLE(TIME_SCALE_STABILITY_OFFSET, 74, 50, 200);
TUNABLE(TIME_SCALE_STABILITY_POWER, -269, -400, -150);

TUNABLE(TIME_SCALE_SCORE_CHANGE_DIVISOR, 425, 10, 1000);
TUNABLE(TIME_SCALE_POS_SCORE_SCALE, 124, 50, 200);
TUNABLE(TIME_SCALE_NEG_SCORE_SCALE, 107, 50, 200);

TUNABLE(TIME_SCALE_SCORE_OFFSET, 85, 10, 200);
TUNABLE(TIME_SCALE_SCORE_SCALE, 48, 10, 90);
TUNABLE(TIME_SCALE_SCORE_MIN, 62, 40, 100);
TUNABLE(TIME_SCALE_SCORE_MAX, 174, 120, 1000);

TUNABLE(TIME_SCALE_MIN, 12, 1, 100);

static constexpr Int32 TIME_SCALE_STABILITY_MIN_DEPTH = 6;

TUNABLE(EVAL_ADJUST_PAWN_SCALE, 46, 20, 200);
TUNABLE(EVAL_ADJUST_KNIGHT_SCALE, 434, 300, 700);
TUNABLE(EVAL_ADJUST_BISHOP_SCALE, 442, 300, 700);
TUNABLE(EVAL_ADJUST_ROOK_SCALE, 656, 400, 1000);
TUNABLE(EVAL_ADJUST_QUEEN_SCALE, 1185, 800, 1600);
TUNABLE(EVAL_ADJUST_MATERIAL_BASE, 26630, 10000, 40000);
TUNABLE(EVAL_ADJUST_OPTIMISM_SCALE_BASE, 1086, 0, 12000);
TUNABLE(EVAL_ADJUST_OPTIMISM_SCALE_MATERIAL_SCALE, 824, 0, 2048);
TUNABLE(EVAL_ADJUST_HALF_MOVE_SCALE, 176, 101, 400);

TUNABLE(THREAD_WEIGHT_SCORE_OFFSET, 12, 0, 20);

TUNABLE(MOVE_ORDER_GOOD_NOISY_MARGIN_SCORE_SCALE, 309, 0, 1024);
TUNABLE(MOVE_ORDER_GOOD_NOISY_MARGIN_OFFSET, 71, -384, 384);

TUNABLE(MOVE_ORDER_NOISY_SCORE_SCALE, 135, 0, 512);

TUNABLE(MOVE_ORDER_DIRECT_CHECK_BONUS, 5806, 2048, 16384);
TUNABLE(MOVE_ORDER_DIRECT_CHECK_MARGIN, -73, -300, 150);

TUNABLE(TT_CUTOFF_MAX_HALFMOVES, 90, 75, 99);

TUNABLE(TT_CUTOFF_BONUS_DEPTH_SCALE, 236, 128, 512);
TUNABLE(TT_CUTOFF_BONUS_OFFSET, 454, 0, 768);
TUNABLE(TT_CUTOFF_BONUS_MAX, 2928, 1024, 4096);

TUNABLE(EVAL_POLICY_BONUS_BASE, 629, 0, 2048);
TUNABLE(EVAL_POLICY_BONUS_GAIN_SCALE, 9, 0, 20);
TUNABLE(EVAL_POLICY_BONUS_MIN, -1616, -4096, 0);
TUNABLE(EVAL_POLICY_BONUS_MAX, 2602, 0, 4096);

TUNABLE(POST_LMR_BONUS_DEPTH_SCALE, 273, 128, 512);
TUNABLE(POST_LMR_BONUS_OFFSET, 625, 0, 768);
TUNABLE(POST_LMR_BONUS_MAX, 2774, 1024, 4096);

TUNABLE(POST_LMR_PENALTY_DEPTH_SCALE, 310, 64, 512);
TUNABLE(POST_LMR_PENALTY_OFFSET, 210, 0, 768);
TUNABLE(POST_LMR_PENALTY_MAX, 1077, 768, 4096);

TUNABLE(QUIET_BONUS_DEPTH_SCALE, 375, 128, 512);
TUNABLE(QUIET_BONUS_OFFSET, 280, 0, 768);
TUNABLE(QUIET_BONUS_MAX, 2451, 1024, 4096);

TUNABLE(QUIET_PENALTY_DEPTH_SCALE, 402, 128, 512);
TUNABLE(QUIET_PENALTY_OFFSET, 131, 0, 768);
TUNABLE(QUIET_PENALTY_MAX, 1263, 1024, 4096);

TUNABLE(NOISY_BONUS_DEPTH_SCALE, 276, 128, 512);
TUNABLE(NOISY_BONUS_OFFSET, 470, 0, 768);
TUNABLE(NOISY_BONUS_MAX, 2376, 1024, 4096);

TUNABLE(QUIET_MOVE_NOISY_PENALTY_DEPTH_SCALE, 393, 128, 512);
TUNABLE(QUIET_MOVE_NOISY_PENALTY_OFFSET, 56, 0, 768);
TUNABLE(QUIET_MOVE_NOISY_PENALTY_MAX, 1627, 1024, 4096);

TUNABLE(NOISY_MOVE_NOISY_PENALTY_DEPTH_SCALE, 306, 128, 512);
TUNABLE(NOISY_MOVE_NOISY_PENALTY_OFFSET, 167, 0, 768);
TUNABLE(NOISY_MOVE_NOISY_PENALTY_MAX, 1491, 1024, 4096);

TUNABLE(PCM_QUIET_BONUS_DEPTH_SCALE, 144, 64, 512);
TUNABLE(PCM_QUIET_BONUS_OFFSET, 39, 0, 768);
TUNABLE(PCM_QUIET_BONUS_MAX, 1451, 768, 4096);

TUNABLE(PCM_NOISY_BONUS, 59, 0, 2048);

TUNABLE(HISTORY_FDEPTH_STATIC_EVAL_SCALE, 140, 0, 512);

TUNABLE(MAIN_HIST_WEIGHT, 681, 0, 4096);
TUNABLE(PAWN_HIST_WEIGHT, 498, 0, 4096);
TUNABLE(CONT1_HIST_WEIGHT, 1024, 0, 4096);
TUNABLE(CONT2_HIST_WEIGHT, 971, 0, 4096);
TUNABLE(CONT4_HIST_WEIGHT, 750, 0, 4096);
TUNABLE(CONT6_HIST_WEIGHT, 244, 0, 4096);
TUNABLE(CAPTURE_HIST_WEIGHT, 889, 0, 4096);

TUNABLE(MAIN_HIST_UPDATE_WEIGHT, 1039, 512, 2048);
TUNABLE(PAWN_HIST_UPDATE_WEIGHT, 805, 512, 2048);
TUNABLE(CONT1_HIST_UPDATE_WEIGHT, 1068, 512, 2048);
TUNABLE(CONT2_HIST_UPDATE_WEIGHT, 1141, 512, 2048);
TUNABLE(CONT4_HIST_UPDATE_WEIGHT, 870, 512, 2048);
TUNABLE(CONT6_HIST_UPDATE_WEIGHT, 982, 512, 2048);
TUNABLE(CAPTURE_HIST_UPDATE_WEIGHT, 991, 512, 2048);

TUNABLE(CONT_HIST_BASE_MAIN_HIST_WEIGHT, 197, 0, 4096);
TUNABLE(CONT_HIST_BASE_PAWN_HIST_WEIGHT, 411, 0, 4096);
TUNABLE(CONT_HIST_BASE_CONT1_HIST_WEIGHT, 655, 0, 4096);
TUNABLE(CONT_HIST_BASE_CONT2_HIST_WEIGHT, 953, 0, 4096);
TUNABLE(CONT_HIST_BASE_CONT4_HIST_WEIGHT, 531, 0, 4096);
TUNABLE(CONT_HIST_BASE_CONT6_HIST_WEIGHT, 567, 0, 4096);

TUNABLE(CORR_HIST_BONUS_MAX, 281, 64, 2048);
TUNABLE(CORR_HIST_PENALTY_MAX, 284, 64, 2048);

TUNABLE(PAWN_CORR_HIST_WEIGHT, 49, 16, 128);
TUNABLE(FRIENDLY_NONPAWN_CORR_HIST_WEIGHT, 41, 16, 128);
TUNABLE(ENEMY_NONPAWN_CORR_HIST_WEIGHT, 36, 16, 128);
TUNABLE(MINOR_PIECE_CORR_HIST_WEIGHT, 30, 16, 128);
TUNABLE(MAJOR_PIECE_CORR_HIST_WEIGHT, 35, 16, 128);

TUNABLE(CONT1_CORR_HIST_WEIGHT, 71, 16, 128);
TUNABLE(CONT2_CORR_HIST_WEIGHT, 65, 16, 128);
TUNABLE(CONT4_CORR_HIST_WEIGHT, 50, 16, 128);
TUNABLE(CONT6_CORR_HIST_WEIGHT, 32, 16, 128);

static constexpr Int32 WINDOW_MIN_DEPTH = 3;

TUNABLE(WINDOW_INIT_DELTA, 5, 1, 50);
TUNABLE(WINDOW_SQUARED_SCORE_SCALE, 55, 0, 1024);
TUNABLE(WINDOW_FAIL_HIGH_FREDUCTION, 101, 0, 768);
TUNABLE(WINDOW_MAX_FREDUCTION, 388, 0, 896);
TUNABLE(WINDOW_WIDENING_SCALE, 138, 1, 256);

static constexpr Int32 OPTIMISM_MIN_DEPTH = 2;

TUNABLE(OPTIMISM_SCORE_SCALE, 142, 75, 300);
TUNABLE(OPTIMISM_DIVISOR_OFFSET, 110, 50, 200);

TUNABLE(HINDSIGHT_FEXT_MIN_FREDUCTION, 401, 0, 1024);
TUNABLE(HINDSIGHT_FEXTENSION, 94, 0, 512);
TUNABLE(HINDSIGHT_FRED_MIN_FDEPTH, 232, 128, 1024);
TUNABLE(HINDSIGHT_FRED_MIN_FREDUCTION, 234, 0, 1024);
TUNABLE(HINDSIGHT_FRED_MARGIN, 206, 0, 500);
TUNABLE(HINDSIGHT_FREDUCTION, 113, 0, 512);

TUNABLE(IIR_MIN_FDEPTH, 361, 128, 1024);
TUNABLE(IIR_TT_FDEPTH_OFFSET, 378, 0, 1024);
TUNABLE(IIR_FREDUCTION, 122, 0, 512);

TUNABLE(RFP_MARGIN_LINEAR_DEPTH_SCALE, 32, 0, 128);
TUNABLE(RFP_MARGIN_QUADRATIC_DEPTH_SCALE, 5, 0, 25);
TUNABLE(RFP_MARGIN_IMPROVING_SCALE, 18, 0, 150);
TUNABLE(RFP_MARGIN_COMPLEXITY_SCALE, 153, 0, 512);
TUNABLE(RFP_MAX_FDEPTH, 1455, 0, 2560);
TUNABLE(RFP_FAIL_FIRM_T, 763, 0, 1024);

TUNABLE(RAZORING_MARGIN_DEPTH_SCALE, 241, 0, 512);
TUNABLE(RAZORING_MAX_FDEPTH, 519, 0, 1280);
TUNABLE(RAZORING_FULL_ALWAYS_MAX_FDEPTH, 112, 0, 256);
TUNABLE(RAZORING_FULL_MAX_FDEPTH, 256, 0, 512);
TUNABLE(RAZORING_FULL_MARGIN, 156, 0, 400);
TUNABLE(RAZORING_FRED_MAX_FDEPTH, 373, 0, 768);
TUNABLE(RAZORING_FREDUCTION, 124, 0, 512);

TUNABLE(NMP_MARGIN_BASE, 191, 0, 400);
TUNABLE(NMP_MARGIN_DEPTH_SCALE, 1348, 0, 2560);
TUNABLE(NMP_MARGIN_IMPROVING_SCALE, 34, 0, 100);
TUNABLE(NMP_MIN_FDEPTH, 456, 0, 1280);
TUNABLE(NMP_FRED_BASE, 458, 0, 1024);
TUNABLE(NMP_FRED_FDEPTH_SCALE, 167, 0, 1024);
TUNABLE(NMP_FRED_STATIC_EVAL_DIFF_SCALE, 81, 0, 128);
TUNABLE(NMP_FRED_STATIC_EVAL_DIFF_MAX, 376, 0, 768);
TUNABLE(NMP_NO_VERIF_MAX_FDEPTH, 1927, 1024, 3072);
TUNABLE(NMP_NO_VERIF_MARGIN, 34, 0, 200);
TUNABLE(NMP_MIN_PLY_FDEPTH_SCALE, 108, 0, 128);

TUNABLE(PROBCUT_BETA_OFFSET, 341, 0, 500);
TUNABLE(PROBCUT_BETA_IMPROVING_SCALE, -22, -250, 250);
TUNABLE(PROBCUT_FREDUCTION, 403, 0, 768);
TUNABLE(PROBCUT_MIN_FDEPTH, 868, 0, 1536);
TUNABLE(PROBCUT_SEE_STATIC_EVAL_DIFF_SCALE, 125, 0, 256);

TUNABLE(MINI_PROBCUT_BETA_OFFSET, 421, 0, 800);
TUNABLE(MINI_PROBCUT_TT_FDEPTH_OFFSET, 396, 0, 1024);

TUNABLE(IIR2_MIN_FDEPTH, 281, 128, 1024);
TUNABLE(IIR2_FREDUCTION, 91, 0, 384);

static inline MultiArray<Int32, 2, 256, 256> LMR_TABLE;

namespace LMR {

static void init();

}

TUNABLE_CALLBACK(LMR_NOISY_BASE, -6, -50, 75, []() { LMR::init(); })
TUNABLE_CALLBACK(LMR_NOISY_DIVISOR, 251, 150, 350, []() { LMR::init(); })
TUNABLE_CALLBACK(LMR_QUIET_BASE, 76, 50, 125, []() { LMR::init(); })
TUNABLE_CALLBACK(LMR_QUIET_DIVISOR, 244, 100, 300, []() { LMR::init(); })

static inline void LMR::init() {
    LMR_TABLE.fill({});

    const Float64 noisyBase = Utils::floatDiv100(LMR_NOISY_BASE);
    const Float64 noisyDivisor = Utils::floatDiv100(LMR_NOISY_DIVISOR);
    const Float64 quietBase = Utils::floatDiv100(LMR_QUIET_BASE);
    const Float64 quietDivisor = Utils::floatDiv100(LMR_QUIET_DIVISOR);

    for (Int32 depth = 1; depth < 256; depth++) {
        for (Int32 moves = 1; moves < 256; moves++) {
            const Float64 lnDepth = std::log(static_cast<Float64>(depth));
            const Float64 lnMoves = std::log(static_cast<Float64>(moves));
            LMR_TABLE[0][static_cast<USize>(depth)][static_cast<USize>(moves)] = static_cast<Int32>(1024.0 * (noisyBase + lnDepth * lnMoves / noisyDivisor));
            LMR_TABLE[1][static_cast<USize>(depth)][static_cast<USize>(moves)] = static_cast<Int32>(1024.0 * (quietBase + lnDepth * lnMoves / quietDivisor));
        }
    }
}

TUNABLE(LMR_FDEPTH_TT_PV_SCALE, 741, 0, 1536);

static inline MultiArray<Int32, 2, 256> LMP_TABLE;

namespace LMP {

static void init();

}

TUNABLE_CALLBACK(LMP_TABLE_BASE, 3, 0, 20, []() { LMP::init(); })

static inline void LMP::init() {
    LMP_TABLE.fill({});

    for (Int32 improving = 0; improving < 2; improving++) {
        for (Int32 depth = 0; depth < 256; depth++) {
            LMP_TABLE[static_cast<USize>(improving)][static_cast<USize>(depth)] = (LMP_TABLE_BASE + depth * depth) / (2 - improving);
        }
    }
}

TUNABLE(LMP_MARGIN_HISTORY_SCALE, 712, 0, 2048);

TUNABLE(QUIET_HISTORY_PRUNING_MAX_FDEPTH, 625, 0, 1280);
TUNABLE(QUIET_HISTORY_PRUNING_MARGIN_DEPTH_SCALE, -2577, -4096, 0);
TUNABLE(QUIET_HISTORY_PRUNING_MARGIN_OFFSET, -1244, -4096, 4096);

TUNABLE(QUIET_FP_MARGIN_BASE, 241, 0, 500);
TUNABLE(QUIET_FP_MARGIN_DEPTH_SCALE, 57, 0, 128);
TUNABLE(QUIET_FP_MARGIN_HISTORY_DIVISOR, 92, 32, 384);
TUNABLE(QUIET_FP_MAX_FDEPTH, 944, 0, 2048);

TUNABLE(NOISY_HISTORY_PRUNING_MAX_FDEPTH, 484, 0, 1280);
TUNABLE(NOISY_HISTORY_PRUNING_MARGIN_DEPTH_SCALE, -845, -4096, 0);
TUNABLE(NOISY_HISTORY_PRUNING_MARGIN_OFFSET, -1550, -4096, 4096);

TUNABLE(BNFP_MARGIN_BASE, 280, 0, 500);
TUNABLE(BNFP_MARGIN_DEPTH_SCALE, 75, 0, 128);
TUNABLE(BNFP_MARGIN_HISTORY_DIVISOR, 106, 32, 384);
TUNABLE(BNFP_MAX_FDEPTH, 1059, 0, 2048);

TUNABLE(CAPTURE_FP_MARGIN_BASE, 362, 0, 600);
TUNABLE(CAPTURE_FP_MARGIN_DEPTH_SCALE, 355, 0, 512);
TUNABLE(CAPTURE_FP_MAX_FDEPTH, 934, 0, 2048);

TUNABLE(SEE_PRUNING_MARGIN_QUIET_DEPTH_SCALE, -18, -100, -1);
TUNABLE(SEE_PRUNING_MARGIN_NOISY_DEPTH_SCALE, -116, -150, -1);
TUNABLE(SEE_PRUNING_MARGIN_NOISY_HISTORY_DIVISOR, 67, 12, 256);

TUNABLE(SE_MIN_FDEPTH_BASE, 671, 0, 1280);
TUNABLE(SE_MIN_FDEPTH_TT_PV_SCALE, 98, 0, 512);
TUNABLE(SE_TT_FDEPTH_OFFSET, 375, 0, 1024);
TUNABLE(SE_BETA_MARGIN_BASE, 85, 0, 256);
TUNABLE(SE_BETA_MARGIN_PREV_PV_SCALE, 91, 0, 256);

TUNABLE(SE_DOUBLE_FEXT_MARGIN_BASE, 9, -50, 150);
TUNABLE(SE_DOUBLE_FEXT_MARGIN_PV_SCALE, 111, 0, 300);
TUNABLE(SE_DOUBLE_FEXT_MARGIN_NEW_PV_SCALE, 51, 0, 100);
TUNABLE(SE_DOUBLE_FEXT_MARGIN_COMPLEXITY_SCALE, 1078, 0, 8192);

TUNABLE(SE_TRIPLE_FEXT_MARGIN_BASE, 107, 0, 300);
TUNABLE(SE_TRIPLE_FEXT_MARGIN_PV_SCALE, 336, 0, 800);
TUNABLE(SE_TRIPLE_FEXT_MARGIN_NEW_PV_SCALE, 54, 0, 100);
TUNABLE(SE_TRIPLE_FEXT_MARGIN_NOISY_TT_MOVE_SCALE, 208, 0, 500);
TUNABLE(SE_TRIPLE_FEXT_MARGIN_COMPLEXITY_SCALE, 1321, 0, 8192);

TUNABLE(SE_QUADRUPLE_FEXT_MARGIN_BASE, 256, 0, 500);
TUNABLE(SE_QUADRUPLE_FEXT_MARGIN_PV_SCALE, 560, 0, 1200);
TUNABLE(SE_QUADRUPLE_FEXT_MARGIN_NEW_PV_SCALE, 65, 0, 150);
TUNABLE(SE_QUADRUPLE_FEXT_MARGIN_NOISY_TT_MOVE_SCALE, 287, 0, 800);
TUNABLE(SE_QUADRUPLE_FEXT_MARGIN_COMPLEXITY_SCALE, 1180, 0, 8192);

TUNABLE(SE_SINGLE_FEXTENSION, 200, 0, 384);
TUNABLE(SE_DOUBLE_FEXTENSION, 115, 0, 384);
TUNABLE(SE_TRIPLE_FEXTENSION, 70, 0, 256);
TUNABLE(SE_QUADRUPLE_FEXTENSION, 79, 0, 256);
TUNABLE(SE_FAIL_HIGH_NEG_FEXTENSION, 394, 0, 768);
TUNABLE(SE_CUT_NODE_NEG_FEXTENSION, 222, 0, 640);
TUNABLE(SE_FAIL_LOW_NEG_FEXTENSION, 73, 0, 256);
TUNABLE(SE_SCORE_NEG_FEXTENSION, 48, 0, 256);

TUNABLE(MULTICUT_FAIL_FIRM_T, 570, 0, 1024);

TUNABLE(MLDE_MAX_FDEPTH, 1250, 0, 2560);
TUNABLE(MLDE_MARGIN, 15, 0, 150);
TUNABLE(MLDE_FEXTENSION, 73, 0, 256);

TUNABLE(LDSE_MAX_FDEPTH, 967, 0, 2048);
TUNABLE(LDSE_SINGLE_FEXT_MARGIN, 31, 0, 150);
TUNABLE(LDSE_DOUBLE_FEXT_MARGIN, 23, 0, 300);
TUNABLE(LDSE_DOUBLE_FEXT_TT_FDEPTH_OFFSET, 370, 0, 1024);
TUNABLE(LDSE_SINGLE_FEXTENSION, 130, 0, 384);
TUNABLE(LDSE_DOUBLE_FEXTENSION, 154, 0, 384);

TUNABLE(CUT_NODE_FRED_MIN_FDEPTH, 764, 0, 1536);
TUNABLE(CUT_NODE_FREDUCTION, 31, 0, 256);

static constexpr Int32 LMR_MIN_MOVES = 2;

TUNABLE(LMR_MIN_FDEPTH, 262, 0, 1024);
TUNABLE(LMR_FRED_OFFSET, 425, -2048, 2048);
TUNABLE(LMR_FRED_QUIET_HISTORY_SCALE, 398, 0, 768);
TUNABLE(LMR_FRED_NOISY_HISTORY_SCALE, 416, 0, 768);
TUNABLE(LMR_FRED_NON_PV_SCALE, 1079, 0, 3072);
TUNABLE(LMR_FRED_TT_PV_SCALE, 1173, 0, 3072);
TUNABLE(LMR_FRED_IMPROVING_SCALE, 1139, 0, 3072);
TUNABLE(LMR_FRED_GIVES_CHECK_SCALE, 825, 0, 3072);
TUNABLE(LMR_FRED_CUT_NODE_SCALE, 1886, 0, 3072);
TUNABLE(LMR_FRED_TT_PV_FAIL_LOW_SCALE, 1087, 0, 3072);
TUNABLE(LMR_FRED_ALPHA_RAISES_SCALE, 575, 0, 3072);
TUNABLE(LMR_FRED_NOISY_TT_MOVE_SCALE, 999, 0, 3072);
TUNABLE(LMR_FRED_MOVES_TRIED_SCALE, 48, 0, 128);
TUNABLE(LMR_FRED_COMPLEXITY_SCALE, 492, 0, 2048);

TUNABLE(DEEPER_SEARCH_MARGIN_BASE, 32, 0, 150);
TUNABLE(DEEPER_SEARCH_MARGIN_DEPTH_SCALE, 4, 0, 20);
TUNABLE(DEEPER_SEARCH_FEXTENSION, 170, 0, 384);

TUNABLE(SHALLOWER_SEARCH_MARGIN_BASE, 0, 0, 150);
TUNABLE(SHALLOWER_SEARCH_MARGIN_DEPTH_SCALE, 2, 0, 20);
TUNABLE(SHALLOWER_SEARCH_FREDUCTION, 111, 0, 384);

TUNABLE(PCM_WEIGHT_BASE, 132, -1024, 1024);
TUNABLE(PCM_WEIGHT_DEPTH_SCALE, 420, 0, 1024);
TUNABLE(PCM_WEIGHT_DEPTH_MAX, 3785, 0, 8192);
TUNABLE(PCM_WEIGHT_PREV_MOVES_TRIED_SCALE, 1054, 0, 2048);
TUNABLE(PCM_WEIGHT_PREV_MOVES_TRIED_MIN, 7, 1, 21);
TUNABLE(PCM_WEIGHT_PREV_TT_MOVE_SCALE, 958, 0, 2048);
TUNABLE(PCM_WEIGHT_STATIC_EVAL_SCALE, 931, 0, 2048);
TUNABLE(PCM_WEIGHT_PREV_STATIC_EVAL_SCALE, 1118, 0, 2048);
TUNABLE(PCM_WEIGHT_MARGIN, 125, 0, 250);
TUNABLE(PCM_WEIGHT_PREV_MARGIN, 139, 0, 250);

TUNABLE(QSEARCH_FAIL_FIRM_T, 664, 0, 1024);
TUNABLE(QSEARCH_FP_MARGIN, 139, 0, 400);
TUNABLE(QSEARCH_SEE_PRUNING_MARGIN, -31, -2000, 200);

static constexpr Int32 QSEARCH_MAX_MOVES = 2;

inline void TunableList::init() noexcept {
    SEE::init();
    LMR::init();
    LMP::init();
}

}
